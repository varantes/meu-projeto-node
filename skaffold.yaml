apiVersion: skaffold/v2beta17
kind: Config
metadata:
  name: meu-projeto
build:
  artifacts:
    - image: gcr.io/validid-vpass-sandbox/meu-projeto
      context: .
      docker:
        dockerfile: Dockerfile
deploy:
  kubectl:
    manifests:
      - deployment.yaml
